// Re-export from central config for backwards compatibility
export {
  PROGRAM_IDS,
  RPC_URL,
  USDC_MINT,
  PROTOCOL_TREASURY,
  NETWORK,
  RUNTIME_URL,
  getExplorerUrl,
} from './network-config';
