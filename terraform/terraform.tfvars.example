# Copy to terraform.tfvars and fill in values

aws_region       = "us-east-1"
environment      = "dev"
domain_name      = "api.blockhelix.xyz"
hosted_zone_name = "blockhelix.xyz"
container_port   = 3002
cpu              = 512
memory           = 1024
desired_count    = 1
enable_https     = false  # Set to true for production
